import 'package:essentials/modal/custom_dialog.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('CustomDialog pode ser instanciado', () {
    expect(CustomDialog(), isA<CustomDialog>());
  });
}
